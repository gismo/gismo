// Pre-existing gismo_EXTENSIONS entry for the sources_vendored_twice case: proves
// a dedup'd append to that CACHE INTERNAL list does not clobber what was already there.
int gsdeptest_extension_seed() { return 7; }
